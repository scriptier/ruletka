# Task: Document or extend blurMode unit edges

## Goal
Extend `mobile/src/prefs/blurMode.test.mjs` with 1–2 edge cases if missing (invalid mode, default).

## Scope
- `mobile/src/prefs/blurMode.test.mjs` (+ blurMode source if pure helper)

## Done criteria
- [ ] dev-smoke --unit PASS
- [ ] RESULT + COMPLETE

## Do not
- MediaSession / deploy
