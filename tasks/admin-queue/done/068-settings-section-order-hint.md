# Task: Settings privacy section short intro

## Goal
Above the blur mode chips in `mobile/app/settings.tsx`, ensure the section label + hint read clearly (Off default). If a one-line intro is missing, add soft hint via existing keys only.

## Scope
- `mobile/app/settings.tsx` (copy/layout only around blur section)
- en.json / ru.json only if needed

## Done criteria
- [ ] Blur section scannable; default Off obvious
- [ ] RESULT + COMPLETE

## Do not
- Media prefs store / connect path
