# Task: Friends list row a11y

## Goal
Audit friend rows in `mobile/app/friends.tsx` for accessibilityRole/Label on call/add actions. Minimal fix only.

## Scope
- `mobile/app/friends.tsx` (a11y props only — no layout rewrite)
- en/ru if keys needed

## Done criteria
- [ ] Primary actions labeled
- [ ] RESULT + COMPLETE

## Do not
- Hub protocol / deploy / bulk rewrite
