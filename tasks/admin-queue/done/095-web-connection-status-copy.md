# Task: Web Connection status microcopy audit

## Goal
Audit `ui/live.html` Connection Status section for short clear labels (no CSS rewrite). Fix only obvious hardcoded EN if i18n keys exist.

## Scope
- `ui/live.html` Connection section, `ui/i18n/en.json` + `ru.json` if keys needed

## Done criteria
- [ ] Status strings clear or documented OK
- [ ] RESULT + COMPLETE

## Do not
- WebRTC / force_relay / deploy
