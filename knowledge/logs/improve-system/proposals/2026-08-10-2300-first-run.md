# improve-system proposals — first run (2026-08-10)

Sources reviewed: AGENTS.md, SCHEMA, wiki index/log, active specs, GOTCHAS, new skills, recent product thrash (mobile UX + linking speed + mute spam screenshot).

### P1: Keep mute UI single-source in smoke checklist
WHY: Screenshot photo_2026-08-10_16-30-59 showed "They muted you" on stage + banner + bottom (spam). Code path moved to LiveStatusBanners; smoke list should refuse GOAL_MET if multi-banner.
WHERE: `docs/DEVICE_SMOKE.md` or `knowledge/specs/current-mobile-ux.md` EVAL
CHANGE: Add EVAL bullet: at most one they-muted surface visible mid-match.
RISK: low
APPROVE?: approved

### P2: improve-system weekly cadence note in AGENTS
WHY: Marchese Step 3 only works if run repeatedly; first-run log exists but AGENTS does not hint cadence.
WHERE: `AGENTS.md` Knowledge section
CHANGE: One line: “After multi-day thrash or weekly: `/improve-system`.”
RISK: low
APPROVE?: approved

### P3: Link linking-speed VERIFY to product.ok smoke gate
WHY: Speed hop1/hop2 land without deploy/smoke; easy to claim faster while black-cam regresses.
WHERE: `knowledge/specs/current-linking-speed.md` CHECKPOINTS (already has smoke) + skill `av-fix-loop` or AGENTOS note
CHANGE: Explicit “no GOAL_MET on speed without av-verify product.ok after UI deploy + APK”.
RISK: low
APPROVE?: approved

### P4: Resource funnel for user screenshots by default
WHY: UX screenshots lived only in Downloads/Pictures; agents re-discovered thrash from chat memory. `/add-new-resource` now exists — habit must stick.
WHERE: `AGENTS.md` Always bucket or mobile UX lane
CHANGE: Always: when human drops screenshot of bugs, run `/add-new-resource` before coding.
RISK: low
APPROVE?: approved

### P5: Optional — session-start pointer to active specs
WHY: Subagents sometimes thrash without reading `current-*.md`.
WHERE: `.grok/skills/agentic-engineering/SKILL.md` or AGENTS preflight
CHANGE: One-liner: before multi-step work, list active specs from `knowledge/specs/current-*.md`.
RISK: low
APPROVE?: approved

### P6: DENY candidate — do not auto-run improve-system every chat
WHY: Would spam proposals and burn tokens; Marchese wants deliberate loop.
WHERE: n/a
CHANGE: none — document as never auto-fire without user ask or scheduled human cadence.
RISK: n/a
APPROVE?: denied (by design — not a change request)
