# Emu smoke — 2026-08-10

**ROLE:** emu-smoke  
**Result:** DONE (script OK) with **product FAIL items** (routing / Start→Live not verified)  
**AVD:** `ruletka_api34` (API 34, `sdk_gphone64_x86_64`)  
**GPU:** `swiftshader_indirect`  
**Serial:** `emulator-5554`

## APK

| Field | Value |
|-------|--------|
| Path | `/home/drakosik/freenet-roulette/mobile/artifacts/ruletka-0.1.310-vc318.apk` |
| versionName | **0.1.310** |
| versionCode | **318** |
| minSdk / targetSdk | 24 / 35 |
| Package | `me.ruletka.app` |
| Scheme | `ruletka` |

## Boot / install

| Check | Result |
|-------|--------|
| Boot success | **Y** — `sys.boot_completed=1` (~seconds after cold start; device appeared quickly) |
| Install success | **Y** — streamed install Success (after uninstall) |
| Process alive through smoke | **Y** — pid stable (`6080` during `emu-test`; later restarts ok) |
| Fatal JS / useHub crash | **N** observed |

Full script log: `/tmp/ruletka-emu-test.out`  
Emulator log: `/tmp/ruletka-emu.log`  
Command: `mobile/scripts/emu-test.sh` → **`emu-test OK` exit 0**

## Screenshots (this run)

| Shot | Path | What it shows |
|------|------|----------------|
| deeplink-pre-rules | `mobile/artifacts/emu-deeplink-pre-rules.png` | 18+ rules under **system ANR** “Process system isn't responding” |
| home | `mobile/artifacts/emu-home.png` | Home OK: hub, stars, CTA, blur tip |
| friends | `mobile/artifacts/emu-friends.png` | Friends code / invite UI |
| settings | `mobile/artifacts/emu-settings.png` | **Same Friends shell** + “unknown friend code” toast (settings route fail) |
| live | `mobile/artifacts/emu-live.png` | **Friends UI**, not live room (live route fail) |
| after-start | `mobile/artifacts/emu-after-start.png` | Still Friends (Start control missing) |
| home-return | `mobile/artifacts/emu-home-return.png` | Home again |
| primary copy | `mobile/artifacts/emu-screenshot.png` | Copy of home |
| probe-live | `mobile/artifacts/emu-probe-live.png` | Extra: `ruletka://live` → Friends |
| probe after Start chatting | `mobile/artifacts/emu-probe-after-start-chat.png` | Android share sheet (invite text) |

UI dumps: `mobile/artifacts/emu-ui-{deeplink,onboard,live,after-start}.xml`

## Script steps (emu-test)

1. **Deep link pre-rules** `ruletka://live` — ALIVE; no `useHub outside` / error boundary.  
   - **WARN:** expected age-gate strings in dump; dump only saw ANR dialog (`Close app` / `Wait`). Screenshot still shows age gate **under** ANR.  
2. **Onboarding** — taps `Yes, I'm 18 or older`, `Don't allow` (×2 notifications?), `Got it` → home.  
3. **friends / settings / live** deep links — process ALIVE; screenshots prove friends OK, **settings & live mis-route**.  
4. **Start tap** — no `content-desc="Start"` / `text="Start"` on live dump → fallback `540 2130`.  
   - After: `Next=False Stop=False Start=False` → **WARN non-fatal**.  
5. **home-return** — ALIVE.  
6. Package dump confirms **0.1.310 / 318**.

## UI text found (home / a11y)

### Present
- **Stars:** `★0`, `0 stars`, `Hub online · ★0 · 5 online · 0 waiting`
- **Blur help:** `Live: eye = privacy blur · mute partner · leave app ok · long-press name to copy`
- **Start CTAs (home):** `Start chatting`, `Start matching — they may join the queue`
- **Hub / pool:** `Hub online`, `Pool is quiet — invite a friend`, `5 online, nobody else waiting…`
- **Identity:** `Your code 5EE4C832`, build `0.1.310 · 318`
- **Nav labels:** `Friends & Call`, `Match prefs & name`, `Privacy` / `Terms` / `Safety`
- **18+:** age gate on cold open; home copy `18+ · no classic account`

### Absent / not reached
- **Looking** — never entered matching/search UI
- **location / Location** — no location UX text in dumps
- **Live room controls** `Start` / `Next` / `Stop` (content-desc) — not on screen after deep link or fallback tap
- **Autostart** into live queue after deeplink — no

### Extra probe: “Start chatting”
Tapping home **Start chatting** opened the **system share sheet** (invite: `Add me on ruletka · code 5EE4C832` + `https://ruletka.vip/live.html?friend=…` + `ruletka://friend/…`), not the live matching room. Quiet-pool UX may be intentional for “invite first”, but smoke cannot verify Start→Next/Stop.

## Limitations

- Single emulator; **no real second peer** → no WebRTC match, ICE, or A/V frames.
- Camera/mic granted via `pm grant`; no real camera pipeline validation.
- Emulator cold path showed **system_server ANR** overlay once (not app crash) — flaky a11y dumps at first paint.
- `emu-test` treats missing Start→Next as **WARN**, so script can **OK** while product routing is wrong.
- No Play integrity / store AAB path; local APK only.
- **Do not thrash ICE** from this run — no crash and no peer session.

## FAIL items (for agents)

1. **`ruletka://live` does not open Live room** — lands on Friends (code/invite). Fix expo-router deep link / path (`live` vs friends). Smoke `open_route "live"` + `emu-live.png` prove it.  
2. **`ruletka://settings` mis-routes** — screenshot is Friends + toast `unknown friend code (friend must connect once)`. Settings deep link or path alias wrong.  
3. **Start→Next/Stop unverified on 0.1.310** — live a11y has no `Start`; fallback tap does nothing useful. Update product a11y (`content-desc="Start"`) **and/or** smoke to open real live UI then tap `Start chatting` / matching CTA.  
4. **Home “Start chatting” → share sheet** when pool quiet — confirm product intent. If intentional, smoke must use **“Start matching — they may join the queue”** (or force non-quiet) to reach Looking/Start/Stop. If not intentional, fix CTA wiring.  
5. **Location UX** — no location strings in UI dump; either not shipped, gated, or unreachable from home/settings route (settings broken). Confirm and fix entry if required.  
6. **Age-gate dump reliability** — first paint blocked by system ANR; script WARN. Optional: dismiss ANR / wait for idle before dump; not an app crash.  
7. **Friends deep-link side effect** — friend-code field sometimes shows garbage from path segments (`FRIENDS` / route leakage). Sanitize deep-link params so unknown path parts do not become “friend code”.

## PASS notes (keep)

- Install + launch + process survival through multi-route smoke.  
- Pre-age-gate deep link does **not** show `useHub outside` / “Something went wrong”.  
- Home renders hub status, star chip, blur tip, version chip.  
- Friends invite/code UI usable (share/copy/add).  
- No ICE/WebRTC code changes made this run.

## Repro

```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Android/Sdk}"
export PATH="$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools:$PATH"
APK=/home/drakosik/freenet-roulette/mobile/artifacts/ruletka-0.1.310-vc318.apk
# emulator already up preferred; else:
# nohup emulator -avd ruletka_api34 -no-snapshot-save -no-audio -gpu swiftshader_indirect > /tmp/ruletka-emu.log 2>&1 &
cd /home/drakosik/freenet-roulette/mobile
./scripts/emu-test.sh "$APK" 2>&1 | tee /tmp/ruletka-emu-test.out
```

---

**summary=** APK 0.1.310/vc318 install+boot OK; `emu-test` exit 0; home/stars/blur OK; **live+settings deep links show Friends**; Start→Next not reached; no peer A/V; FAILs are routing/CTA/smoke alignment not ICE.
