# Spec: RU users without VPN

```text
GOAL: Users in Russian Federation can load the site and complete 1v1 A/V without a VPN, without breaking FRA same-LAN force_relay lock.
DONE WHEN:
  - Human in RU (no VPN) loads https://ruletka.me (or vip) and WSS connects
  - 1v1 faces both ways OR we have a measured FAIL (site vs TURN)
  - product.ok still holds for FRA same-WiFi force_relay=true
  - pool=0; no force_relay=false; 480 not cut
EVAL:
  - Split: HTTPS 200 vs WSS vs ICE/TURN
  - av-verify after any TURN URL change
MUST NOT: SFU; docker coturn; PUBLIC/VPC_PRIVATE; invent product.ok
```

Facts: hub+site+TURN are one DigitalOcean FRA IP. RKN/DPI often filters DO AS + UDP. VPN works because it exits the filter. TURNS:443 mux is live (sslh ALPN → Caddy 8443).

Measure 2026-08-22 (check-host RU DC, not residential):
- ru1/ru2/ru3 ping + TCP 443 + TCP 5349 to 209.38.204.153 all OK
- HTTP :80 → 308 (Caddy redirect) all 3
- HTTPS /health: ru1 Moscow AS14576 **200**; ru2/ru3 AS210644 **Broken pipe** (likely non-ALPN TLS sent to coturn by sslh — browsers send h2 ALPN)
- Voidly/OONI: no samples for ruletka.vip

Next: human RU **phone ISP** (MTS/Beeline/MegaFon), no VPN — page vs linking vs black cam. Second front IP only if the **page** fails on residential. Do not buy a droplet from DC probes alone.
