# Gotchas (connect thrash)

Canonical: `.grok/skills/av-fix-loop/references/GOTCHAS.md` (17 items after 2026-08-10). Wiki thrash list through **75**.

Highest signal from the PC-black saga:

1. Score first — product field, not theories  
2. Signaling/TURN PASS can still be one-way  
3. Layer A pure / B bind / C encode — fix the failing layer only  
4. closeCall(keepLocal) must not wipe force_relay sticky  
5. bind_v≥1 ≠ frames_out (force encodings.active)  
6. pool=0 forever  
7. One writer agent; rebuild APK if dual-edit  
8. GOAL_MET only with product.ok or human faces  

## Mobile UX thrash (2026-08-11)

9. **SurfaceView eats overlays** — put partner name/★/loc in a flex dock **below** stage, not only absolute chrome over video.  
10. **Never unmount RTC for blur** — keep stream mounted; opaque Modal only while veiled.  
11. **max_mto ≥ 20s is web/cache first** — hard-refresh `live.js?v=` + stuck-offer hop; do **not** ship more HUD APKs.  
12. **APK flood without smoke is vibe coding** — at most one verify-gated APK, then human paste-back (`SMOKE-NEXT`).  
13. **Stars display** = max(stars, trust) or ★0 looks “broken” when only trust is set.  
14. **Spec install lines must match artifacts** — update `SMOKE-NEXT` when version bumps or agents install the wrong APK.
15. **PC chrome-autohide** — do **not** “fix” vanished Settings by forcing `chrome-always` / pin forever. Keep local rail visible only while `.is-chrome-open` or settings/flyout sheet open. See [live-chrome-ux](live-chrome-ux.md).
16. **Partner ★0 mid-match is often correct** — hub `MatchPeer.stars`/`trust` are serde `u64` always present; stranger with no gifts → known 0. Self gift-bar ★N is *your* spendable balance, not partner. Diagnose with logcat `[match]` / `[geo]` (`known=1` + both 0 = empty ledger; `known=0` = omitted field / merge keep). Loc blank only when hub has no geo yet (private IP / lookup pending) or hide_ip — flag/country/city when present must paint under name on top strip.
17. **PC dual id / hex / friend_code as name** — never paint `partner_short` / `short_id` / `friend_code` / bare 6–12 hex as conversationalist name (`resolvePartnerPaintName` → real name or generic "Partner" only). Hub `resolve_match_peer_name` empty > hex. On DC `partner_identity`, **re-paint** via `paintPartnerIdentityChrome` (not only `lastMatchMeta`). Always hide `#remote-who-sub` on 1v1 (CSS + `hideRemoteWhoSub` — no "Незнакомец · CODE" dual-id). Ship stamp **`live.js?v=551`**. Skill: `mobile-match-identity`.
18. **CONNECT / Link offer toast ban on Android release** — no mid-match stopwatch toast for CONNECT/Link offer timing. `MediaSession` still emits `CONNECT offer=…` on first frame for log; `live.tsx` routes that string to **log + `lastConnectStats` → Settings Last connect only** (no `showToast`).
19. **Brand watermark place differs by client** — **PC:** `data-brand-seam` from **live people** (not leftover `.stage-trio`, not `#remote-third:not([hidden])`). **duo** 1v1/hunt = 50% partner\|you; **trio** 3 live = 33% partner\|3rd; **quad** 4-way/2×2 = 50% stage cross. Soft **360° every 30s**. `showThirdConnecting` unhides `#remote-third` on 1v1 and used to slide the pill off the seam. **Android:** `BrandWatermark` **bottom-middle** of partner video; same 360°/30s. Skill: `brand-stage`. Pin **`live-brand.css?v=19`** · **`live.js?v=620`**.

## Multi-party 3/4-way (2026-08-12)

20. **Chrome Android multi freezes cam** — never put the **same** video `MediaStreamTrack` on 2+ `RTCPeerConnection`s. Primary keeps real preview track; secondaries **`track.clone()`** (`outboundVideoTrackForPc`). Audio push must **not** `setLocalStream(preview)` (re-binds shared video and kills cam). See [multi-party](multi-party.md).
21. **Multi encode: do not floor mobile at min** — 3–4 way extras at **min** freeze Android Chrome HW; floor extras at **low** on mobile Chrome/WebView (`applyMultiPeerOutboundQuality`). Cap is **3 opponents** (you + 3), not 2 — keep `log.partyCap` i18n honest. Secondary Android PC: `keepLocal` / adopt stream — do not `stop()` shared tracks when secondary closes.
22. **Find-3rd accept freezes partner (PC sees frozen conversationalist)** — Android must **not** flip `multiRemote` for hunt-only (unmounts 1v1 RTCView). Hunt = wrap 1v1 VideoView + bottom looking strip (phone **column** mid-split). Multi-audio / encode thrash only when `nPeers ≥ 2`, not mere `party_browse`. PC after `enableTrioLayout`: `softKeepPartnerPaint` / soft re-play `#remote`, no null-rebind. Skill: **`multi-party-stage`**. Ship tip (disk): Android **0.1.366-vc374**, `live.js?v=561`.
23. **Find-3rd full-screen brand (no partner video) on Android** — `live.tsx` must handle `find_third_result` → `huntingWithPartner` + `party_browse` while **keeping** `remoteStream` and phase **matched** (never pure search). Wire `lookingForThird` from that flag. PC: force `has-remote-feed` + hide `#remote-empty` during trio-searching so partner column stays live. Ship tip (disk): **0.1.366-vc374**, `live.js?v=561`, `live-stage.css?v=386`.
24. **Local pair-smoke wire 0/0 ≠ phone fail** — `scripts/pair-smoke.mjs` hardcodes `ROULETTE_TURN=off`. Same-IP match still wants pure `force_relay` → clients thrash `force_relay no TURN — fail-open all` + `offer no relay — rebuild pure-relay PC`; many `localSdp offer`, **zero** `wsOut offer/answer`, hub mto/mta null, rematch never reached (T1 EXIT 2, 2026-08-12). Phone WEB_PAIR device-smoke can still be **MATCHED**. Do not invent product/phone FAIL from harness residual alone; fix harness TURN (or document SKIP) before treating wire budgets as product signal. See T1/T2 pair-smoke RESULTs.
25. **Never mirror `connectUi.ts` in tests** — `test-connect-ui.mjs` once copied `nextAutoRetryAction` with **soft@10s** (forbidden; lock is **soft ≥16s / hard ≥26s / tip ≥36s**). Tests must transpile the **real** module (`test-geo-localize.mjs` pattern). See [connect-cleanup](connect-cleanup.md).
26. **Outbound watch timers must die on `closeCall`** — flipping `_outboundWatchArmed=false` is not enough; unstored `setTimeout` waves still `pollOutboundVideo` / re-bind after hangup. Store IDs (`_outboundWatchTimers`) and clear. Herd **ms-safe** 2026-08-14.
27. **iOS Safari must not run mic-boost Web Audio / forced 48k** — default **125%** always builds Gain+Compressor+MediaStreamDestination (leaves AEC); GUM `sampleRate:{ideal:48000}` vs WebKit AudioContext often **44100** → resample **crackle** + conversational delay. Meter must not tap the live send track (clone or skip). Do **not** default `lowLatencyAudio=true` globally (kills NS on desktop). Product gate = human iPhone Safari smoke — **no product.ok invent**. Code hop **in progress** herd `20260814T021357Z` (do not invent ship). See [ios-safari-audio](ios-safari-audio.md).
28. **3rd matched, pair still “Looking for a 3rd”** — `handleMatched` must **not** `setThirdSlotStream(null)` after `joinPeers` when a stranger is listed. That re-opens hunt brand + `forceThirdBrandLoop` **nulls `#remote-third`** while ICE is gathering. Use `showThirdConnecting`. Also: hub **one live tab per identity** (second Hello kicks the old tab) — desk 3-way needs 3 distinct logins. Disk **`live.js?v=579`**. **No MULTI_PASS.** See [multi-party](multi-party.md).
29. **3-way skip/stop** — pair Next = next 3rd (keep teammate). 3rd Next = solo search. Pair Stop = remaining+3rd **1v1**. Never treat `mode=party_browse` as “I am the pair” (`your_role===party` only). Android pair Next must not `closeCall` primary. Hub 3rd Next: `leave_keep` + solo queue (disk; needs deploy). Distinct from gotcha **44** (extra peer hangs up → keep remaining pair). **No MULTI_PASS.**
30. **Settings Off NS must go through `audioConstraints()`** — phone `prefs.noiseReduction` default **true**; Settings toggle + `setNoiseReduction` / `applyFullAudioProcessing` re-apply existing tracks. **ns-gum landed** (`audioConstraints()` reads `this.noiseReduction`): Off → next GUM EC/NS/AGC **false**. Hardcoding those three **true** in the helper makes Off a no-op on next GUM. **No RNNoise.** Hiss was **125%** mic-boost Web Audio, not missing NS. Installed APK **0.1.378 / vc386** is pre-NS-hop. Phone smoke still needs APK. **No product.ok invent.**
31. **Hide used to send a black canvas; CSS `filter:blur()` on `<video>` also paints black** (desktop GPU + touch laptops, not just phones). Partner sees a dead tile. Disk `live.js?v=587`: outbound Hide = soft-blurred `captureStream` (never `getBlackVideoTrack`); **never `self_hide` on Hide** (DC-open / updateSideIcons used to re-send hide → laptop covers the feed black). `self_hide` only for cam-off or zero video tracks. Inbound: if remote still has a video track, ignore hide cover. Laptop/PC need **deploy** for this. No product.ok invent.
32. **`anon` is poison — must not inherit the last Courtier.** Latch `lastGoodPartnerName` only for the **same user_id**. 3-way extras must not take top-level `Matched.name` / `partner_short` (that painted two tiles the same). Empty/anon → "Partner" (or Opponent N), never previous match. Disk `live.js?v=588` + `matchPeers.ts`. Phone needs APK. No invent PASS.
33. **SoftBlur sink during ICE = native crash.** Pixel 2026-08-13 23:06 SIGABRT `DecodingQueue` in `libjingle_peerconnection_so` ~2s after `SoftBlurVideoView: Sink attached` while ICE **checking** + intro blur (`heal_matched`). Second VideoSink + decoder start. Disk: SoftBlur only when `mediaReady` (not Linking). APK **0.1.378** still crashes. Needs **build apk**. No invent PASS.
34. **Blur “doesn’t work” = tap-anywhere Modal peel.** Log `toggle` → `Sink attached` → `hide why=blur_modal_anywhere` ~1s. Full-screen Pressable treated every tap as Show video. SoftBlur `removeSink` on toggle left RTCView **black**. Disk: no tap-anywhere; keep sink attached; veil/eye only to peel. Needs **build apk**.
35. **Laptop Chrome ↔ Android ICE failed with relay 1/1** — names/flags (hub) ≠ media. Score `20260814T052039Z`: signaling OK, TURN HOT, app_vc **387**, ice checking→failed, frames 0/0. Two leftovers: (1) phone reused **ice=closed** PC for setRemote (`startCall` treated leftover remoteDescription as live). Disk: `dropDeadPc` / `pc_drop_closed`. Needs **APK**. (2) Laptop is on **prod webrtc 316 / live 573**; disk is **319 / 589**. Audio-only GUM skipped video transceiver (only empty-stream added A+V). Disk webrtc **319** adds video transceiver when no video track. **Needs deploy** for laptop. Do not invent product.ok.
36. **388 dropDeadPc mid-answer = silent + laptop black.** Score `20260814T053228Z` app_vc **388**: answer_sent ice=checking bind_v=1 then ~2s later ice=**new** bind_v=0 (startCall drop on ice=closed blip wiped live PC). No-cam portrait worked (hub/DC). Sound + laptop cam dead. Disk: never dropDeadPc after `inAnsweredCall`. Needs **APK**. No product.ok.
37. **Laptop never ICE-connects to Android** — not a name/flag bug. 381 held answer (checking, bind_v=1) then failed. Hub: web offer `video_dir=recvonly` relay=1, android answer sendrecv relay=1, no selected pair. Laptop still prod webrtc **316**. **382/383 spread + recvonly→no-cam REGRESSED PC↔Android** (answer relay=3, Android no-cam on real PC cam, PC Linking). Reverted spread/recvonly latch. Restore PC path first. No product.ok.
38. **3-way laptop sees PC, not Android** — tiles ≠ two live PCs. Hub party 1v2 lists **stranger first**; `handleMatched` keep-PC used `peers[0]` + `needNewPeer` + `forceFlip=force_relay&&!live` → every 3rd-join was a **full rematch** that tore the teammate PC and re-offered. Hub **per-client** 3.5s offer debounce drops the second offer → `have-local-offer` + frames_in stuck. Disk hop **`live.js?v=590`** (current HTML pin **`?v=598`**): incremental `joinPeers` when any listed PC is live; tear listed PCs only if ice=closed. Needs **rsync**. **No MULTI_PASS.** See [multi-party](multi-party.md).
39. **4-way mesh cost is 6 P2P links, not SFU** — 4 people = K4 = **6** peer links; each client holds **3** `RTCPeerConnection`s and encodes cam up to 3× (primary mid, extras low). Same-LAN / same public IP is hub `force_relay` → **FRA coturn** (up to 3 TURN allocations per client) — **not** broken P2P. **Lock stays** — never `force_relay=false` on same IP. Phone HW encode + clone is the risk; desktop Chrome usually holds. Cap **3 opponents (you+3)** is mesh budget (`log.partyCap` / `partyCap`). **Human 3-way faces still OPEN — no MULTI_PASS.** Disk web pins (`ui/live.html` 2026-08-14 fin-wiki): `live.js?v=598` · `webrtc.js?v=324` · stage **413**. See [force-relay-same-lan](force-relay-same-lan.md) · [multi-party](multi-party.md).
40. **Hub per-client offer debounce drops 3rd offer** — `Client.last_offer_at` is **one Instant per sender**. In a 1v2 (`party vs solo`, `n_targets` can be 2) the same client sends two offers in &lt;3.5s to **different** `to=` peers. Hub treats the second as thrash and drops it → **answers &lt; offers** (desk `071810Z`: offers=2 answers=1 · black tiles · you sliver · hex **AC8BEC2F**). Same-peer 3.5s debounce must stay (blocks rematch/watchdog doubles). Disk hop **p-hub** (`bridge/src/simple.rs`): debounce **per target peer**, not per sender. **Needs deploy** (human compile/restart — agents must not `push.sh`). Distinct from gotcha **38** (web rematch tore teammate; `live.js` incremental join). **No MULTI_PASS.** See [multi-party](multi-party.md).
41. **2×2 only at 4 people; laptop double-PC = same stream twice** — tile count **equals people**. 1v1 (2 tiles) and 3-way (3 tiles) must **not** show a 2×2 control. 2×2 exists **only** when there are 4 people (optional switch; same four, one per cell, no empty fifth). Laptop seeing **PC cam twice** and no Android cam is **not** a 2×2 layout — it is the **same remote stream bound to two tiles** (gotcha **38** rematch + empty 3rd column). Treat as FAIL. Android 1v1 leftover black `075019Z` (frames_in leftover ≠ paint) is a separate 1v1 paint residual, not MULTI_PASS. Spec [`2026-08-14-3way-see-everyone.md`](../specs/2026-08-14-3way-see-everyone.md). Herd `20260814T080020Z`. **No MULTI_PASS.** See [multi-party](multi-party.md).
42. **3/4-way Stop dock is a stage child, not a tile-1 overlay** — `#tile-floor-partner` (Stop / Next / chat) lives under `<main#stage>` **after** all tiles. **1v1** stays **partner-column** only. **3-way / 4-way** = reserved band **under all tiles** (`--stage-dock-h` + stage `padding-bottom`) so the dock does **not** cover faces. Do **not** move the floor back inside `#tile-remote` or `position:fixed` over tile 1. 2×2 still **only at 4**. Disk pins (re-read `ui/live.html` 2026-08-14 fin-wiki): `live.js?v=598` · stage **413** · i18n **252**. Herd `20260814T082214Z`. **No MULTI_PASS.** See [multi-party](multi-party.md).
43. **MATCHED 3-way still hunt Find-3rd mid + you sliver + silent** — banner **MATCHED / You + friend / stranger on the right** is **not** 3-way faces. Mid column still hunt **Find 3rd**; left Courrier **black**; you crushed to a sliver; no sound. Phone leftover 1v1 Partner ★0 **black**. av-verify `084452Z` product **one-way** (web 6227/6234 · android **0/0** ice **checking** · app_vc **392** · offers=3 answers=2). Tab is **prod ruletka.vip** — disk hops need **rsync** (agents must not `push.sh`). 1v1 leftover ≠ 3-way. **No MULTI_PASS** / **no invent product.ok**. Spec [`2026-08-14-3way-messed-novideo.md`](../specs/2026-08-14-3way-messed-novideo.md). Pins after sibling wait: `live.js?v=598` · `webrtc.js?v=324` · stage **414**. Herd `20260814T084616Z`. See [multi-party](multi-party.md).
44. **3rd leave must keep remaining pair; Relayed same-LAN ≠ lost P2P** — extra peer bye/stop closes **only** that PC. Remaining remote keeps `srcObject` / RTCView; layout **3→2**; no `closeAllPeers` rematch; Android extra hangup must **not** `startCall` primary. Same-Wi-Fi **Relayed** = designed **FRA TURN** (`CONNECTIVITY_LOCK`) — **never** recommend `force_relay=false` on same public IP (host hairpin black). Signaling is still **mesh P2P**, **not SFU**. Badge Relayed ≠ “we lost P2P.” **This is the disk hop** (keep-web / keep-android) — **not** human PASS. Spec [`2026-08-14-keep-pair-no-relink.md`](../specs/2026-08-14-keep-pair-no-relink.md). Herd `20260814T085708Z`. **No product.ok invent.** See [force-relay-same-lan](force-relay-same-lan.md) · [multi-party](multi-party.md).
45. **Android first paint then Linking then black** — face can land from `startCall EARLY` while `Matched` still has `keepPrimary=false` (phase was search). Same handler then applies intro veil + SoftBlur second sink (gotcha **33**), `setConn("connecting")`, “Partner video shown” peel toast, and sometimes `setRemoteStream(null)` / `forceRepaintRemote`. Video **starts then stops**. Latch: `shouldClearRemoteUi` keeps leftover only for **this** partner or first-match race; **new** partner still clears. Do **not** veil-after-paint. Hide Linking pill once remote audio/video is live. Disk hop only — phone still needs **install** after next APK. Spec [`2026-08-14-android-first-paint.md`](../specs/2026-08-14-android-first-paint.md). **No product.ok invent.**
46. **Intro SoftBlur peel kills the face.** SoftBlur second sink paints the 2–3s intro; peel hides it and RTCView never started (or remount/Linking toast). 387 still: blur → Linking popup → gone. **Intro = PartnerBlurVeil over live RTCView only** (`allowSoftBlur` false). SoftBlur is hold/eye only. Never toast Linking on intro peel. Needs **APK**. Raw `2026-08-14-android-blur-then-linking.md`. **No product.ok invent.**
47. **SoftBlur peel onto opaque `blurCoverElevated` = face then gone.** Wrapper stayed mounted with `backgroundColor: #45536c` + elevation 32. Intro 2.8s SoftBlur then `setActive(false)` revealed the slate slab, not RTCView. Human 2026-08-14: right blur 3–4s then disappeared. Disk: `softBlurCover` (transparent) while on, `softBlurIdle` (1×1, elev 0) after peel; **no** `forceRepaintRemote` on `intro_*`. SoftBlur stays mounted (gotcha **34** removeSink). Browser Hide / Reveal / Blur them: `#local` / `#remote` lock `opacity:1 !important` + z 20/5 (also inline in `live.html`); joinPeers policy force-revealed mid-call. Disk: `applyLocalHideCover` inline !important + `selfBlurUserLocked`. Pins `live.js?v=605` · stage **422**. Needs **rsync**. **No product.ok invent.**
49. **iPhone Safari + PC 1v1 then Android as 3rd = Android isolated.** Two browsers stay host→host (they see each other). Android ice=failed 0/0 — sees nobody; browsers do not see Android. iPhone hangup → 2 tiles, PC sees Android again (3→2 keep works). Inverted mix vs usual Android+PC then laptop. Dual browser offers can both hit Android media1 before secondaryPeerId latch. Score `214914Z`. Spec `2026-08-14-iphone-pc-android-3way.md`. Herd `20260814T215311Z`. **Role note (`221232Z`):** laptop Chrome **3-col** vs this PC **2-col** is **ROLE** (YOU Laptop vs a pair vs YOU Драконов 1v1 dock), not a different app — phone still 1v1 Laptop ★31 black. Distinct from this isolation mix. **No MULTI_PASS.**
48. **PC first match black, rematch paints — overlay, not ICE.** av-verify can be **product.ok** (frames both ways) while Courtier is black. Inbound video tracks start **muted** until first RTP; treating that as audio-only + CSS `#tile-remote.has-no-cam-portrait #remote-no-cam[hidden] { display:flex; z-index:9 }` covers `#remote`. Never show `[hidden]` no-cam. `muted` ≠ dummy. Clear no-cam once `videoWidth>0` / `partnerVideoEverFrames`. Shots `214314Z` fail · `214534Z` rematch ok. Disk `live.js?v=627` · stage **442**. Needs **rsync**. **No GOAL_MET.**
50. **Laptop 3-col vs this PC 2-col is ROLE, not a different app.** `221232Z`: laptop YOU **Laptop** ★31 banner **vs a pair** (3-col: Courtier black | Драконов live | self no-cam). This PC YOU **Драконов** still **2-col** 1v1 dock (Courtier black | self live · Connecting…). Phone `221222Z` 1v1 Laptop ★31 **black**. Do not treat as two Chrome builds / two apps. Raw `2026-08-14-laptop-vs-pc-chrome-3way.md`. Gotcha **49** note. **No MULTI_PASS.**
51. **Dummy inbound + has-remote-feed hides the no-cam cylinder.** CDP `231251Z`: `#remote` is a **muted live** track with **videoWidth=0** yet `#tile-remote.has-remote-feed` stays on → cylinder hidden, **Laptop black**. Distinct from **48** (`muted` ≠ dummy; hidden no-cam overlay covering real video). Dummy / brand-loop / muted-zero-width must **not** latch `has-remote-feed` or `partnerVideoEverFrames`; after ~1.5s `videoWidth=0` show no-cam and **drop** has-remote-feed. Mid no-cam cylinder on this PC was OK; phone **idle**. av-verify `231255Z` product **one-way** web 2045/2053 · android **0/0** app_vc **None**. Shots `231251Z-now3-*`. Herd `20260814T231459Z` **p-wiki**. **No MULTI_PASS.**
52. **Never promote answerer mid multi.** Offer watchdog (`_armOfferWatchdog`) flipping `isOfferer=true` on a live 1v1 when find-3rd starts tears the first PC. `232252Z`: console **fin=121** → find-3rd + `[webrtc] offer watchdog — promote answerer → offerer` → **fin=0** `ice_failed` + `keep-pair drop ice_failed kept=1` + **null pc** `onconnectionstatechange` (`webrtc.js:2389` `this.pc.connectionState`). Paint hop **did** work (two no-cam cylinders + you live). Phone stayed **1v1** no-cam. Distinct from **40** (hub debounce) and **38** (stranger rematch). Watchdog may promote only if never answered / not connected / not multi. Score `232302Z` product **one-way** web 1886/1892 · android **0/0** app_vc **None**. Shots `232252Z-tear3-*`. Herd `20260814T232343Z` **r-wiki**. **No MULTI_PASS.**
53. **Ended remote tracks + leftover videoWidth ≠ a camera.** CDP `234930Z` (pins **634/328**): `#remote` 427×320 `has-remote-feed` but both tracks **ended** → Courtier **black slab** (cylinder blocked because `setNoCamPortrait` treated leftover WxH as pixels). 3rd-join `ice_failed` on the **first** PC used `dropOnePeerKeepRest` and left that dead stream bound. Distinct from **51** (muted live dummy, width=0) and **52** (watchdog promote). Also: 1v2 first offer `video_dir=recvonly` when preview map lags `#local`. Disk **`live.js?v=635` · `webrtc.js?v=329`**. Phone still APK **0.1.392** 1v1. **No MULTI_PASS.**
55. **3-way names: left = first conversationalist, mid = 3rd.** `015545Z`: left live cam labeled **Laptop**, mid no-cam labeled **Partner**. Hub lists stranger first (`opponents[0]`) + `lastMatchMeta` primary = stranger + joinPeers `addIds` hex into `extraSlotDisplayName` → stolen Courtier latch and mid "Partner". Paint `#remote` from `pickFirstPartnerPeer` only; mid from `pickExtraRemotePeer` real name. Never latch the 3rd as Courtier. Disk **`live.js?v=646`**. Do not thrash 1v1 ICE. **No MULTI_PASS.**
54. **3rd-tile gate must not be ICE-up, and must not use map size.** 642 `thirdPersonConnected` (2 ICE-connected PCs + live A/V) hid the mid tile when a 3rd was found (`013444Z`, extra `ice=new`). 638 leftover `listed=2` / `#tile-third:not([hidden])` CSS opened a ghost mid column on working 1v1. **Lock:** 1 unique PC = 2 tiles (1v1 **and** hunt). 2 unique PCs = show mid tile (`ice=new` ok). Dual-key of the same 1v1 PC must stay duo. Do **not** call `enableTrioLayout(false)` on listed=2 before `joinPeers` adds the extra PC. Do **not** thrash `webrtc.js` to “fix” this tile. Disk **`live.js?v=645`**. **No MULTI_PASS.**
56. **Hub 3rd-join omit-primary remounts Android onto the 3rd.** `030650Z`: PC 3-col Courtier **frozen** (`ice=failed`) · phone **1v1** no-cam Partner. Hub Matched often `peers=[Laptop]` only (or stranger-first). Old `pickPrimaryPeerIndex` returned **0** → `startCall` tore Courtier. Disk: `keepFirstOnOmit` → `-1` keep prevPrimary (`matchPeers.ts` + `live.tsx`). APK **0.1.395 / vc403** **not smoked**. Also `030415Z` phone already **Start** while PC still 3-col — lobby-spin is a second FAIL. Distinct from **52** (watchdog) / **53** (drop first PC). **No MULTI_PASS.**
57. **Frame counters ≠ a camera.** `034125Z` 1v1: phone Dragonov **black**, PC you-tile black, av-verify product.ok. web bytes_out ~2 kB/s vs bytes_in megabytes. 649 Hide sample returned `#local` at `readyState>=2` with **videoWidth=0** → navy fills still increment frames. frost `show()` bailed once. Disk **`live.js?v=650`**. Do not ICE. **No product.ok invent.**
58. **Second getUserMedia kills CaptureThread.** `21:50:59` on **0.1.395**: new GUM while Camera2 `onConfigured` still running → `Session has been closed` → app dies to launcher. 394 skipped GUM only when `multiPeerAudio`. 396 skipped **all** liveV — 399 1v1 Let's go then `fout=0` (PiP + Courtier black; PC→phone still grew) because `zero_f_*` could not re-GUM a stale live track. **Lock:** skip GUM on secondary/multi and on 1v1 live **unless** `zero_f_*`/`weak_*`. Then **stop old video first**, wait ~80ms, then GUM. Never GUM while another session is in `gumOwners`. **No ICE.** **No product.ok invent.**
59. **Hunt-keep HELD; no-cam must not cover a live PC; laptop miss is not Chrome-version.** `201649Z`: Android did **not** freeze or disconnect while searching for a 3rd — hunt-keep is a **WIN**; **do not remount RTC**. After 3rd joined: phone two-stack OK; PC cam painted then **NoCamPortrait covered** the live tile (top Драконов ★205 + Laptop chip + “No camera · Talking with microphone · Драконов”). Bottom laptop cylinder is **correct** (laptop has no webcam). Same Chrome on PC and laptop: **PC good**; laptop sees PC **not** Android — role / unanswered 3rd offer (`answers < offers`, web `ice=new` 0/0), **not** a Chrome-version bug. Distinct from **38** (rematch tore teammate), **40** (hub per-client debounce), **48** (muted ≠ dummy). Leftover av-verify product.ok is **1v1**, not this 3-way. **No MULTI_PASS.** Shots `201649Z-3way-smoke2-phone` · `201554Z-3way-smoke-pc-live`. Spec [`2026-08-15-3way-held-nobreak.md`](../specs/2026-08-15-3way-held-nobreak.md). Overlay hops 651; mesh-merge hop **`live.js?v=652`**. Human **0.1.406 / vc414**: split + 2 conversationalists + **PC video stays on phone** — progress, **not MULTI_PASS**. Laptop→Android still open until they confirm. **Not** ICE / force_relay / hunt remount. See [multi-party](multi-party.md).
64. **Laptop 3rd left tile “Party of 2” + no debate.** P2P-only debate never reached the 3rd (no extra DC); hub chat (`\x01debate:`) is the room fan-out. Identity DC defaulted ★ onto `#remote`. Disk **`live.js?v=785`**. Android hub parse/send on disk — **needs APK**. Distinct from **50** (role), **55** (name swap), **59** (unanswered 3rd). **No MULTI_PASS.**

64b. **Laptop 3rd left tile “Party of 2” + no debate (paint).** `handleMatched` used `setSplitRemote(opponents>=2 && !trioBrowse)` — classic stacked 1v2 — and painted `friends.partyTag` on `#remote-name`. Mid got the one live camera + real name; left stayed black. `liveDebateRoster` read `lastMatchMeta.peers` (always empty — lastMatchMeta is the primary person). Laptop never in speakers; invite/overlay/SFX never shown. Disk **`live.js?v=784`** · stage **535**: stack only at 4 people; names/★ per tile (trust); roster from `lastMatchedPeers`; invite toast centered. Distinct from **50** (role 3-col vs 2-col), **55** (name swap), **59** (unanswered 3rd). **No MULTI_PASS.**

63. **Laptop no-cam recvonly offer + Android sendrecv answer = Chrome InvalidAccessError.** Console `0616 00:32` webrtc **334**: `offer first-relay count=1` then `answer apply failed … Incompatible send direction`. `_ensureNoCamVideoRecvonly` is intentional (empty sendrecv → Android dummy). Phone `forceVideoSendrecvSdp` still answers sendrecv. ICE stays `new`. Disk **`webrtc.js?v=335`**: align answer video to **sendonly** before setRemote. Do not drop no-cam recvonly. No APK. Distinct from **62** (TCP relay). **No product.ok.**
62. **Same-IP laptop ICE fail can be TCP relay vs phone UDP.** After Wi‑Fi fix (`061315Z`): both `162.157.227.46`, `force_relay=true`, latch `hub_fr=1 policy=relay relay=1/1`, still ICE **failed** 0/0. Coturn at match: **TCP** ALLOCATE `:web`, no usable UDP pair. Chrome exits wait on first `typ relay` (often TCP). Disk **`webrtc.js?v=333`**: wait/strip/trickle **UDP** relay under force_relay. Do not flip lock / pool. **No product.ok until smoke.** Distinct from **61** (split IP).
61. **Laptop↔phone Linking with `force_relay=false` is split public IP, not ICE lock.** Score `060913Z`: Pixel `96.1.124.150` vs laptop/PC web `162.157.227.46` (both Calgary). Hub same-IP rule never fires. Phone left fibre at **05:28** (earlier today same pair was `force_relay=true`). ICE checking→failed 0/0; android answer **relay=0**. Recvonly on no-cam laptop is expected. **Do not** always-on relay / pool>0. Human: same TELUS Wi-Fi as laptop until hub shows android `162.157.227.46`. Distinct from **59** (role / Chrome-version). See [force-relay-same-lan](force-relay-same-lan.md) · raw `2026-08-16-laptop-android-split-ip.md`. **No product.ok.**
60. **Paint hops cannot invent Android frames; leftover pair ≠ laptop two videos.** Human **665 nothing changed**. Left Dragonov **black**; mid Courtier name held, **no Android video**. Score `012303Z` app_vc **422**: this PC `42298fc0` offerer=1 connected 1173/1589 + android primary bind_v=1 1690/1263 is the **held pair**. Laptop `58493127` ice=**checking** 0/0 **offerer=0** only — never beacons offerer=1. Android extra ice=**new** bind_v=0 0/0 waits as answerer. Web vs mobile → **laptop must offer**. Paint 662–665 cannot steal a stream that never arrived. Distinct from **59** (role / unanswered 3rd / no-cam cover), **38** (rematch tore teammate), **52** (never promote 1v1 answerer), **661** stuck-extra reset. Offer hop herd `20260816T012351Z` spec [`2026-08-16-laptop-offer-android.md`](../specs/2026-08-16-laptop-offer-android.md). **No APK this hop.** **No MULTI_PASS.** Do not invent product.ok from leftover 1v1.
65. **Browser blur on a new stranger = navy/blue slab, not frost.** `applyStrangerIntroBlur` sets `partner-blurred` + `peer-slot-blurred` immediately. Leftover CSS hid `#remote` before frost-vid had a frame; `captureStream` first frames were `#2a3342` fill. Backdrop-filter `::after` then sampled empty tile → blue screen. **Lock:** keep the live face until frost-vid `.is-on`; start `captureStream` after first `drawSoftBlurredVideo` ok. Disk **`live.js?v=819`** · stage **550**. Distinct from **31** (Hide black canvas) / **47** (Android peel slab). Needs **hard-refresh**. **No product.ok invent.**
66. **Friend phone call is 1v1 until Find 3rd is accepted.** Instant `browse_together` used to throw both into 3-way hunt with no invite. Product: Hangup-only 1v1; hunt strip only after `find_third_result` accepted (`huntingWithPartner`). `isLookingForThird` must **not** treat `findThirdPending` as hunt. Hub on disk rejects friend-1v1 `browse_together` (“invite your friend first — use Find 3rd”) — **needs deploy**. Disk **`live.js?v=844+`**. **No MULTI_PASS.**
67. **Gift FX frozen in Chrome = non-interpolable keyframes.** Do **not** animate `top`/`bottom`/`left` together with `transform`. CSS-only balloons stayed frozen on prod **561**. **Fix that smoked:** canvas rAF (balloons/snow/confetti/flowers/disco/spotlight) + **UI deploy**. Human 2026-08-20 eve: **disco better; snow/confetti/flowers good; spotlight works; Please stay works** (`861/576`). Freeze shot: `knowledge/raw/2026-08-19-confetti-stuck-band.png`. **No invent GOAL_MET.**
68. **Please stay is a partner action, not a gift-strip card.** No “1× / month per person” copy on the stars sheet. Entry: tap-face **Partner actions** (`#btn-partner-please-stay` / Android `more-please-stay`). Still 30★ / 15s no-Next. Prod **858**. APK **0.1.456**.
69. **Star gift: never celebrate before hub `star_effect` ok.** Optimistic comet looked like a send while wallet did not move; then 10s gift lock looked like a dead tap. Avatar tap: skip rate lock, toast fail, mint `gift:` **bypasses daily cap** + counts as trust. UI-only rsync cannot mint — need hub binary. Human smoked tap-★ **854**. **No invent GOAL_MET.**
71. **Hide and Blur them share one glass veil — never `filter:blur()` on `<video>` and never hide the live layer for frost.** Cheap Blur-them (canvas frost-vid + hide `#remote` + dark `::after`) looked like pixels, not Hide. Hide-nice = live `<video>` + `backdrop-filter: blur(26px) saturate(0.8) scale(1.06)` (`#self-hide-veil` / `#tile-remote.partner-blurred::after`). `applyPeerBlurCover` must keep `#remote` visible. GPU `filter` on video still paints black (gotcha **31**). Human 2026-08-21: **nice blur is good now**. Pins **865/581**. Shots `201652Z` / `201842Z`. Distinct from **65** (new-stranger navy slab). Not product.ok.

72. **Stop 1v1 black left tile = leftover `#remote` inline `!important` + first-frame watcher, not missing CSS.** Idle CSS (`html.start-idle #remote-empty`) loses to `el.style.setProperty("opacity","1","important")`. `paintIdlePartnerStage` must stop first-frame / blank watchers, strip inline display/opacity/z-index, `srcObject=null`, hide `#remote`, idle-gate `paintRemoteFromPc` / `markPartnerVideoFramesSeen`. Pins **897/597**. Distinct from **53** (ended tracks). **No product.ok invent.**
73. **Find-3rd hunt is 2v1 or 2v2.** Two 1v1s that both accept Find 3rd enqueue as party2; they **must** match (`stranger_party_blocks_2v2` now only blocks Party3). Solo still joins as 2v1. Live 3-way still does not queue a random 4th (Call / Invite 4th). Same-LAN 2v2 uses `pair_force_relay` — never hardcode `force_relay: false` in `start_party_vs_party`. Spec [`2026-08-21-party-shapes.md`](../specs/2026-08-21-party-shapes.md). Hub binary `boot_id` 543d8f70+. **No MULTI_PASS.**
74. **TURNS 443 mux: `sslh --tls` matches ALL TLS (including TURNS) → Caddy.** Use `/etc/sslh.cfg` ALPN `h2`/`http/1.1` → Caddy **8443**, other TLS → coturn **5349**. Never `tls-listening-port=443` on coturn. Never enable sslh on the workstation (`Drakosik`). Order: Caddy `https_port 8443` then sslh `:443`. Verify: curl+ALPN HTTP 200; no-ALPN GET empty. Check-host “broken pipe” from RU DC is often **no ALPN**, not RKN. Residential phone ISP is the product gate. Spec [`2026-08-20-ru-reach.md`](../specs/2026-08-20-ru-reach.md). **No invent GOAL_MET.**
75. **2v2 per-person actions are PARTIAL.** PC `#remote2` (top-right) has mute/blur but **no tap → partner menu**. Floor chat is room-wide. Debate roster is everyone. Android Add-friend gated on **primary** partnerCode. Gifts work when the action target is set. Distinct from **64** (laptop debate). **No MULTI_PASS.**

70. **1v1 PC↔Android stuck relay wait = android never answers, not the 480 budget.** `20260820T203346Z` FAIL: matches=3 offers=2 **answers=0** — phone never sent an SDP answer. Android av_path pinned `ice=new` / `cs=new` / `sig=stable` the whole window (0/0 both ways), `force_relay_hub=true`, `bind_v=0`, `app_vc=464` (current tip). PC tile read **ruletka.vip loading / linking cameras**; Android UI **Trying relay… 122s** Retry/Rebuild. TURN itself was HOT (`max_rb=641559 peer_hot=61`) — infra fine, only the phone's answer leg is dead. **Do not** read a stuck relay wait like this as the `relayWaitBudgetMs` **480** tuning question (see [linking-speed](linking-speed.md) "do not cut 480") — that lock is about trimming a *working* wait; here there is no answer to wait for. Shots `20260820T203320Z-android-1v1.png` / `-pc-1v1.png`. Raw `20260820T203320Z-1v1-linking.md`. **No product.ok invent.**

### Log

- 2026-08-22: gotchas **72–75** — Stop idle leftover inline paint (**897/597**); Find-3rd 2v1 or 2v2; TURNS 443 sslh ALPN mux; 2v2 tap-menu PARTIAL. **No product.ok / MULTI_PASS invent.**
- 2026-08-21: gotcha **71** — Hide + Blur them = live video + backdrop-filter veil (not canvas frost / not `filter` on `<video>`). Human **nice blur is good now**. Pins **865/581**. **No product.ok invent.**
- 2026-08-20 later2: gotcha **70** — 1v1 PC↔Android FAIL, android answers=0 / ice=new for full window, `20260820T203346Z`. Distinct from **480** relay-wait-budget tuning. **No product.ok invent.**
- 2026-08-20 later: gotcha **69** star spend waits hub ok; canvas FX **858/571**; APK **456** disco club. Balloons+tap ★ smoked. **No product.ok / MULTI_PASS invent.**
- 2026-08-20: gotchas **66–68** — friend call stays 1v1 until Find 3rd accept (**844**, hub reject needs deploy); gift FX transform-only motion (**845/563**, unverified smoke); Please stay in tap-face actions, no monthly copy (**846**). APK tip still **0.1.452**. **No product.ok / MULTI_PASS invent.**
- 2026-08-19: gotcha **65** — new-stranger blur = navy slab. Hop **819/550**. Keep face until frost `.is-on`. **No product.ok invent.**
- 2026-08-18: hop **790** — laptop sees Android not PC: n≥2 force-offerer = web-web glare. Hub per-peer is_offerer only. **No MULTI_PASS.**
- 2026-08-18: gotcha **64** — laptop debate P2P miss; hub `\x01debate:` hop **785**. Android needs APK. **No MULTI_PASS.**
- 2026-08-18: gotcha **64** paint — laptop 3rd Party of 2. Hop **784/535**. **No MULTI_PASS.**
- 2026-08-16: gotcha **63** — laptop console Incompatible send direction; 335 align sendonly. **No product.ok.**
- 2026-08-16: gotcha **62** — same-IP `061315Z` ICE failed; laptop TCP TURN vs phone UDP. webrtc **333**. **No product.ok.**
- 2026-08-16: gotcha **61** — laptop↔Android 1v1 `060913Z` split IP `96.1.124.150` vs `162.157.227.46`; phone left fibre 05:28. Not ICE lock. **No product.ok.**
- 2026-08-16: gotcha **60** — human **665 nothing changed**. Dragonov black + Courtier mid no Android video. Score `012303Z` extra ice=**new** laptop **offerer=0**. Leftover pair ≠ 3rd. Offer hop `20260816T012351Z`. **No APK.** **No MULTI_PASS.**
- 2026-08-16: laptop 3rd `010045Z` **0 inbound cams** vs PC honest pair view (Courtier live + laptop cylinder). Role + missing Android extra + no-cam cover; **not** Chrome-version. Herd `20260816T010337Z`. **No MULTI_PASS.**
- 2026-08-15: gotcha **59** human confirm — laptop **only this PC**, not phone (`230938Z` + paste). Pair product.ok **421**. Android 2nd PC ice=**failed** sig=stable bind_v=1 (offer landed, ICE died) — not the old ice=new wait. Laptop 1 PC (extra likely dropped). Mesh-merge **652/656** ≠ MULTI_PASS.
- 2026-08-15: gotcha **59** — hunt-keep **HELD** `201649Z` (do not remount); PC cam then NoCamPortrait covered top Драконов; laptop same Chrome misses Android (`answers < offers`, ice=new 0/0) ≠ Chrome-version. Leftover product.ok is **1v1**. **No MULTI_PASS.**
- 2026-08-15: gotcha **58** — CaptureThread 395; 396 all-liveV skip → 399 fout=0. 1v1 `zero_f_` may stop+re-GUM; multi still skip. **No product.ok invent.**
- 2026-08-15: gotcha **57** — `034125Z` 1v1 phone black / PC you black; product.ok dummy bytes. 650 sample must have videoWidth. **No product.ok invent.**
- 2026-08-15: gotcha **56** — `030650Z` hub omit-primary remounted Laptop as Android primary → Courtier frozen. keepFirstOnOmit APK **0.1.395**. Also `030415Z` phone Start while PC 3-col. **No MULTI_PASS.**
- 2026-08-15: gotcha **55** — `015545Z` left Laptop / mid Partner. Stranger-first + hex addIds. Hop 646 identity only. **No MULTI_PASS.**
- 2026-08-15: gotcha **54** — 642 ICE-up gate hid 3rd tile (`013444Z`); leftover listed=2 ghosted 1v1 mid column. 644 = unique PC count. Do not re-tighten to `thirdPersonConnected`. **No MULTI_PASS.**
- 2026-08-14: gotcha **53** — `234930Z` ended tracks + leftover 427×320 + drop first-ok PC = black Courtier. Phone 1v1. Hop 635/329 rsync `235308Z`. ≠ **51** / **52**. **No MULTI_PASS.**
- 2026-08-14: gotcha **52** — `232252Z` find-3rd + offer watchdog promote tore **fin=121→0** `ice_failed` + null pc crash. Paint cylinders OK. Phone 1v1 no-cam. Score `232302Z` one-way. ≠ **40** / **38**. **No MULTI_PASS.**
- 2026-08-14: gotcha **51** — CDP `231251Z` dummy inbound (`#remote` muted live **videoWidth=0**) + **has-remote-feed** hides cylinder → Laptop black. Mid cylinder OK; phone idle; fin=0 fout growing. Score `231255Z` one-way. ≠ **48**. **No MULTI_PASS.**
- 2026-08-14: gotcha **49** role note + **50** — `221232Z` laptop **3-col** vs this PC **2-col** is **ROLE** (not a different app). Phone 1v1 Laptop ★31 black. Raw `2026-08-14-laptop-vs-pc-chrome-3way.md`. **No MULTI_PASS.**
- 2026-08-10: expanded after product.ok resolution (layers A/B/C).
- 2026-08-10 walk: do **not** GOAL_MET on linking speed from journal mto/mta alone — need UI deploy + APK hop + `product.ok`. Residual pure max mto~1749 / mta~4097 still pre-hop3c/hop4 ship.
- 2026-08-11: UX thrash gotchas 9–14; smoke APK **0.1.329**; method gap compound.
- 2026-08-11: gotcha **15** PC chrome-autohide restore (`live.js?v=542`); never force always-on forever.
- 2026-08-11: gotcha **16** partner ★0 empty ledger vs bug (0.1.341 screenshot; self gift ★≠partner).
- 2026-08-11: gotchas **17–19** PC dual-id/hex paint + re-paint; CONNECT toast ban; brand bottom-middle / center pill (no fixed TY=160). Skills: mobile-match-identity, brand-stage.
- 2026-08-11 (session): **§17** friend_code also poison; who-sub always off 1v1; **`live.js?v=551`**. **§18** toast ban wired (log + Settings Last connect). **§19 corrected** — PC **center seam** (not tile bottom); Android bottom-middle; `.stage-wm` hidden on PC match.
- 2026-08-12: multi-party gotchas **20–21** — clone secondaries + no audio setLocalStream rebind; mobile multi floor **low** not min; cap 3 opponents. Wiki: multi-party.md.
- 2026-08-12 polish: human smoke stamps now **0.1.363-vc371** / `live.js?v=559` / `webrtc.js?v=315` / stage **v=382** (gotchas **17–23** rules still apply; do not install ancient 0.1.340 / v=544 for smoke).
- 2026-08-12 W4: multi gotchas **20–23** unchanged in rules; ship tip restamp only → **0.1.366-vc374** / `live.js?v=561` / stage **v=385** (no new residual; human multi smoke still open).
- 2026-08-12 W6: multi gotchas **20–23** rules still accurate vs `multi-party-stage` + code; ship tip only §23 stage **v=385→v=386** (`ui/live.html` after W5 hunt caption). Index multi-party link OK. Human multi smoke still open (no PASS invent).
- 2026-08-12 residual: gotcha **24** local pair-smoke TURN=off wire 0/0 thrash — not phone MATCHED fail; T2 rematch residual only.
- 2026-08-12 residual: Claude Code **session limit** ~**12:20am America/Edmonton** — hand off / RESULT stamp, do not thrash past wall. Prod pin lag still **human deploy** (disk **562/386** vs prod **561/384**); multi **open** (no invent PASS). Src `T12-gotcha-session`.
- 2026-08-14: gotcha **27** — iOS Safari must not run mic-boost Web Audio / forced 48k (crackle+delay). Herd `20260814T021357Z`. Code hop in progress; **no product.ok invent.**
- 2026-08-14: gotchas **25–26** — real `connectUi` tests (no 10s-soft mirror); clear outbound watch timers on `closeCall`. Herd `20260814T020528Z`. **No product.ok invent.**
- 2026-08-14: gotcha **30** — Settings Off NS is a no-op until `audioConstraints()` honors the flag; **no RNNoise**. Herd `20260814T042437Z` lane **ns-spec**. Installed APK **0.1.378/vc386** disk-only NS hops. **No product.ok invent.**
- 2026-08-14: gotcha **35** — laptop↔Android ICE failed on closed-PC reuse + laptop prod 316 vs disk 319 audio-only transceiver. **No product.ok invent.**
- 2026-08-14: gotcha **38** — 3-way laptop sees PC not Android: stranger-first rematch tore teammate; disk `live.js?v=590`. **No MULTI_PASS.**
- 2026-08-14: gotcha **48** — PC first-match Courtier black while product.ok; rematch paints. `#remote-no-cam[hidden]` + muted inbound latch. Disk **627/442**. **No GOAL_MET.**
- 2026-08-14: gotcha **44** — 3rd-leave keep remaining pair is the **disk hop**; Relayed same-LAN = designed FRA TURN; lock **stays**; never `force_relay=false` on same IP; mesh P2P not SFU. Spec `2026-08-14-keep-pair-no-relink.md`. Herd `20260814T085708Z` keep-wiki. **No product.ok invent**.
- 2026-08-14: gotcha **47** — SoftBlur intro peel onto opaque `#45536c` cover = right blur 3–4s then gone; Hide `:has(canvas.is-active)` left clear selfie. Disk `softBlurCover`/`softBlurIdle` + `live.js?v=603` / stage **420**. Needs APK + rsync. **No product.ok invent**.
- 2026-08-14: gotcha **43** — MATCHED 3-way still hunt **Find 3rd** mid + you sliver + silent. FAIL `084504Z`. av-verify `084452Z` product **one-way** android **0/0** vc **392**. Tab **prod** — **rsync required**. Pins `live.js?v=598` · stage **414**. Herd `20260814T084616Z` b-wiki. **No MULTI_PASS** / **no product.ok invent**.
- 2026-08-14: gotcha **42** — 3/4-way Stop dock is a **`#stage` child under all tiles**, not tile-1 overlay. 1v1 still partner-column. 2×2 only at 4. Pins `live.js?v=598` · stage **413** · i18n **252**. Herd `20260814T082214Z` fin-wiki. **No MULTI_PASS.**
- 2026-08-14: gotcha **41** — 2×2 control must **not** appear in 1v1/3-way (tile count = people; 2×2 **only at 4**). Laptop double-PC = same stream on two tiles, not a grid. Herd `20260814T080020Z` see-wiki. Spec `2026-08-14-3way-see-everyone.md`. **No MULTI_PASS.**
- 2026-08-14: gotcha **40** — hub **per-client** 3.5s offer debounce drops 3rd offer in 1v2 (`last_offer_at` one Instant per sender). Disk hop **p-hub**; **needs deploy**. Desk FAIL `071810Z` (black tiles · you sliver · hex AC8BEC2F · answers&lt;offers). Herd `20260814T072031Z`. **No MULTI_PASS.**
- 2026-08-14: gotcha **45** — Android first paint then “Partner video shown” / Linking / black is rematch chrome over a live sink, not a new ICE fail. Spec `2026-08-14-android-first-paint.md`. Disk hop; **no product.ok invent**.
- 2026-08-14: gotcha **39** — 4-way mesh cost (6 P2P links / 3 PCs / FRA TURN same-LAN). Herd `20260814T065106Z` trio-wiki. Human 3-way faces **OPEN**. **No MULTI_PASS.**
