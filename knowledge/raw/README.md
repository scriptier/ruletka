# Raw sources (immutable)

Drop smoke notes, av_path pastes, human reports here as **dated files**:

```text
YYYY-MM-DD-topic.md
```

Do **not** edit old dumps — add a new file.  
Process with `/knowledge-ingest` or batch `/knowledge-compound`.
