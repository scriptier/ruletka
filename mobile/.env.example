# Public hub base (HTTPS). WebSocket path is /ws.
EXPO_PUBLIC_HUB_BASE=https://ruletka.vip

# Store contingency: hide stranger Start/Next (friends Call only).
# EXPO_PUBLIC_FRIENDS_ONLY=1

# Optional override after eas init (normally written into app.json).
# EAS_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
